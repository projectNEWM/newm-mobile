object Facebook {
    private const val FLIPPER_VERSION = "0.238.0"
    private const val SOLOADER_VERSION = "0.10.5"

    const val flipper = "com.facebook.flipper:flipper:${FLIPPER_VERSION}"
    const val soloader = "com.facebook.soloader:soloader:${SOLOADER_VERSION}"
    const val flipperNoop = "com.facebook.flipper:flipper-noop:${FLIPPER_VERSION}"
}
